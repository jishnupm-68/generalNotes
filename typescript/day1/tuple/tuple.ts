//array => order not matters
const user : (string | number) [] = [4,'jpm']

// tuple => order and number of items matters in the tuple  , vakue can be change
let data : [string, number, boolean] ;
data =["jis",23, true]

export{}

